FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y build-essential && rm -rf /var/lib/apt/lists/*
COPY meter_reader/services/api/requirements.txt /app/req.txt
RUN pip install --no-cache-dir -r /app/req.txt
COPY . /app